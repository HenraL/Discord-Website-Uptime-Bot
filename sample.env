# The zones marked by [description] are to be replaced with actual values.

# Used in version 1.0.0, 2.0.0
# Insert your bot token here (replace with your own token)
TOKEN=[Your discord bot token]

# Used in version 1.0.0 (These won't cause an error if left in the environement file if loaded in the v2 program)
# The URL of the webpage you want to monitor
WEBSITE_URL=[The website to check]

# Replace with your Discord channel ID (as an integer)
CHANNEL_ID=[The discord channel ID]

# Keyword to verify website content (adjust as needed)
EXPECTED_CONTENT=[The content to locate on the website]

# Used in version 2.0.0
# The path to the file that contains the list of websites to check (only standard json files are supported).
CONFIG_FILE=[The file containing the list of websites to check]  # sample usable file: "config/websites.sample.json"

# The output mode that will be used for the message to be sent on discord, options:
# * RAW: send it as plain text
# * MARDOWN: send it as markdown
# * EMBED: send it as an embedded message
OUTPUT_MODE="MARDOWN"

# Artificial delay (in seconds) inserted between consecutive Discord message
# update operations. Use this to reduce the chance of hitting Discord rate
# limits when the program updates many messages in a short time. Examples:
#  - 0     -> disable the artificial delay (no wait)
#  - 0.1   -> wait 100ms between each update (recommended for bursty updates)
# Provide a float value (seconds). Default: 0
ARTIFICIAL_DELAY=0

# DEBUG mode, set to True to activate, False otherwise, makes the output 10x more verbose as it explains almost every step it is doing.
DEBUG=False
